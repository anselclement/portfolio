{% extends 'base.html.twig' %}

{% block title 'Edition' %}

{% block body %}
    <div class="container" style="margin-top: 150px">
        <h1 class="title is-2">Edition</h1>
        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button class="button is-primary">Editer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}