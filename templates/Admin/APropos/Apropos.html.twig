{% extends 'base.html.twig' %}

{% block title 'A Propos' %}

{% block body %}
    <div class="container" style="margin-top: 150px">
        <h1 class="title is-2">Bienvenue sur la Section A Propos</h1>
        {% for apropos in apropos %}
        <a href="{{ path('aProposAdmin.edition', {id: apropos.id}) }}" class="button is-primary">Edition</a>
        {% endfor %}
        <table class="table">
            <thead>
            <tr>
                <th>Informations</th>
                <th>Actions</th>
            </tr>
            </thead>
            </tbody>
            </tbody>
        </table>
    </div>
{% endblock %}




            
                 
    